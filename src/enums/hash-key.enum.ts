export enum HashKey {
  PATTERN_CLEAN_ALL = '*',
  PRODUCTS = 'products',
  PRODUCT = 'product',
  CART = 'cart',
  USER_EMAIL = 'user_email',
  USER_ID = 'user_id',
  NUMBER_OF_USERS = 'number_of_users',
  USER_CREDENTIALS = 'user_credentials',
}
